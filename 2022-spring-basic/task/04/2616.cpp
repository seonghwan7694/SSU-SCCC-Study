#include <bits/stdc++.h>
using namespace std;
int N, n;
vector<int> A;

int main(){
  ios::sync_with_stdio(false);
  cin >> N;
  A.resize(N+1);
  for(int i = 1; i <= N; i++){
    cin >> A[i];
  }
  cin >> n;




  return 0;
}